# 贡献指南

感谢你考虑为 Claude Skills Hub 贡献技能！

## 贡献流程

1. **Fork** 本仓库
2. 创建你的技能分支：`git checkout -b skill/你的技能名`
3. 在 `skills/` 目录下创建技能文件夹
4. 提交 PR

## 技能结构要求

每个技能必须包含以下文件：

```
skills/你的技能名/
├── SKILL.md      # 技能定义（必须）
└── README.md     # 技能说明文档（必须）
```

### SKILL.md 规范

参考 `skills/_template/SKILL.md` 模板，必须包含：

- **触发条件**：什么场景下 Claude 应该调用此技能
- **执行步骤**：具体的操作流程
- **输出格式**：期望的输出结构
- **依赖说明**：需要的工具、环境或配置

### README.md 规范

技能说明文档应包含：

- 技能用途简介
- 使用示例
- 安装/配置说明
- 注意事项或限制

## 命名规范

- 技能目录名：小写英文，用 `-` 连接，如 `code-review`、`git-changelog`
- 简洁明了，能一眼看出用途

## 质量要求

- [ ] SKILL.md 结构完整，触发条件明确
- [ ] README.md 包含使用示例
- [ ] 不包含硬编码的个人信息（API key、路径等）
- [ ] 不依赖特定操作系统（或在文档中注明）
- [ ] 经过本地测试验证

## 分类标签

提交时请在 README.md 中标注分类：

| 分类 | 说明 |
|------|------|
| `devops` | CI/CD、部署、监控相关 |
| `code-quality` | 代码审查、格式化、规范检查 |
| `documentation` | 文档生成、维护 |
| `productivity` | 效率工具、自动化流程 |
| `notification` | 通知、消息推送 |
| `analysis` | 代码分析、统计 |
